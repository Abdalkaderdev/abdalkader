# Run Build

```bash
cd C:\Users\max\Desktop\react\abdalkader

pnpm turbo run build --filter=@abdalkader/portfolio...

git add .
git commit -m "fix: inline design tokens to avoid CSS import issues"
git push origin main
```
