# Algolia Search Configuration
# Get these from your Algolia dashboard: https://www.algolia.com/
ALGOLIA_SEARCH_API_KEY=your-search-only-api-key
ALGOLIA_ADMIN_API_KEY=your-admin-api-key

# The search-only API key is safe to expose (read-only access)
# The admin API key should NEVER be committed or exposed publicly
