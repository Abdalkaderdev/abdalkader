import { Meta } from '@storybook/blocks';

<Meta title="Introduction" />

# Component Library

Welcome to the **Component Library** documentation. This library provides a collection of accessible, reusable React components built with TypeScript.

## Features

- 🎨 **Themeable** - Built with CSS custom properties for easy theming
- ♿ **Accessible** - WCAG 2.1 AA compliant with proper ARIA attributes
- 📘 **TypeScript** - Full TypeScript support with comprehensive type definitions
- 🎯 **Tree-shakeable** - ES modules support for optimal bundle size
- 📦 **Zero dependencies** - Only peer dependencies on React and React-DOM

## Installation

```bash
npm install @abdalkader/component-library
```

## Usage

```tsx
import { Button, Input } from '@abdalkader/component-library';

function App() {
  return (
    <div>
      <Input 
        label="Email" 
        type="email" 
        required 
      />
      <Button variant="primary" onClick={() => console.log('Clicked!')}>
        Submit
      </Button>
    </div>
  );
}
```

## Components

### Button
A versatile button component with multiple variants and sizes. Supports all standard HTML button attributes.

### Input
A fully accessible input component with label, error states, and helper text support.

## Accessibility

All components are built with accessibility in mind:
- Proper ARIA attributes
- Keyboard navigation support
- Screen reader friendly
- Focus management
- Color contrast compliance

## Theming

Components use CSS custom properties for theming. Override these variables to customize the look:

```css
:root {
  --primary-color: #007bff;
  --border-radius: 0.375rem;
  --font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Roboto', sans-serif;
  /* ... more variables */
}
```

## Browser Support

- Chrome (latest)
- Firefox (latest)
- Safari (latest)
- Edge (latest)

## License

MIT

---

Built with ❤️ for https://abdalkader.dev/